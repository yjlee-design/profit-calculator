# 이익률 계산 프로그램 사용설명서

두 가지 방식이 있습니다. **계산 결과는 완전히 동일합니다** (같은 `engine.py` 를 씁니다).

| | 웹앱 (권장) | 배치 |
|---|---|---|
| 실행 | `웹앱실행.bat` | `실행.bat` |
| 매출 파일 | 화면에서 끌어다 놓기 | `입력/` 폴더에 넣기 |
| 비용 입력 | 화면에서 표로 입력 | `입력/설정.xlsx` 에 입력 |
| 결과 | 화면에서 바로 확인 + 엑셀 다운로드 | `결과/` 폴더에 엑셀 생성 |

---

# 웹앱으로 쓰기

## `웹앱실행.bat` 더블클릭
검은 창이 뜨고 브라우저가 자동으로 열립니다. **검은 창은 끄지 마세요** (끄면 앱도 종료됩니다).
브라우저가 안 열리면 주소창에 `http://localhost:8501` 입력.

## 화면 사용 순서

**왼쪽 사이드바**
- `기준월` — 예 `2026-08`. 결과 파일 이름이 됩니다.
- `기준 파일` — 파일 수정시각과 읽어온 건수가 표시됩니다.
  마진율표가 갱신됐으면 **🔄 업데이트** 를 누르세요.
- `기준 파일 직접 올리기` — 그때그때 다른 파일로 계산하고 싶을 때 (올린 파일이 최우선)

**① 매출 파일** — 이카운트에서 채널별로 받은 파일을 각 칸에 올립니다.
올린 파일은 **그 달에 자동으로 보관**됩니다. 새로고침하거나 나갔다 들어와도
`✅ 보관됨 — 파일명` 으로 남아 있고, 그대로 다시 계산할 수 있습니다.
새 파일을 올리면 그 채널만 덮어쓰고, `지우기` 로 지울 수 있습니다.

**② 비용 입력**
- 카드수수료: 스룩 결제수단별 **정상금액**만 입력 (요율은 그대로)
- 샘플비용: **샘플 발주/지원 리스트 엑셀을 올리면 자동 계산**됩니다 (아래 참고).
  엑셀을 안 올리면 예전처럼 금액을 직접 적으면 됩니다.
- `현재 설정을 입력/설정.xlsx 에 저장` 을 누르면 다음 달에 그 값이 그대로 뜹니다.

### 원가 미매칭 — 직접 넣은 원가는 기억됩니다

기준 파일에 없는 상품은 원가가 `0` 으로 잡혀 **이익이 부풀려집니다.**
결과 화면의 `미매칭` 탭에서 원가를 넣고 **`입력한 원가로 다시 계산 · 기억하기`** 를 누르세요.

- 넣은 원가는 **원가보정에 저장**되어 다음 달에도 자동으로 쓰입니다.
- **`0` 도 넣을 수 있습니다.** 칸을 비워 두면 '아직 안 넣음' 이고,
  `0` 을 적으면 원가가 정말 0원이라는 뜻입니다. (예: 설치옵션 줄)
- 나중에 **기준 파일에 그 코드가 올라오면 기준 파일 값이 우선**하고,
  여기 넣은 값은 자동으로 물러납니다. 지워야 할 것을 기억할 필요가 없습니다.
- 기억하고 싶지 않으면 `이번 계산에만 쓰기` 를 누르세요.

원가를 찾는 순서는 이렇습니다.

```
1. 원가보정 · 우선적용 O      ← 기준 파일보다 앞섬
2. 기준 파일 (최신 연도 먼저)
3. 원가보정 · 우선적용 X      ← 화면에서 넣은 값은 여기
4. 아무데도 없으면 0원 (미매칭)
```

### 샘플비용 자동 계산

샘플 리스트 엑셀을 올리면 이렇게 계산합니다.

1. **날짜** 열을 보고 **기준월에 해당하는 줄만** 고릅니다.
2. **구분**이 `자체부담` 인 줄만 원가에 넣습니다. (협력사부담·업체부담은 제외)
3. **상품명 + 옵션명** 으로 기준 파일(마진율표)에서 **그 달 기준 최신 원가**를 찾습니다.
4. **배송비는 따로** 입력합니다. 파일의 배송비 합계를 미리 채워 두니 필요하면 고치세요.

> 샘플비용 = 자체부담 상품원가 + 배송비

옵션명이 **낱개**로 적혀 있고 기준 파일은 **세트** 단가인 경우도 맞춰 줍니다.
예) 샘플 `[7호-나시-단품]` ↔ 기준 `[7호-나시-3개1세트]` → 세트단가 ÷ 3

원가를 못 찾은 줄은 `원가를 못 찾은 N건 보기` 에 나옵니다.
상품명·옵션명이 기준 파일과 다르면 못 찾으니, 기준 파일에 등록하거나 금액을 직접 고치세요.

**③ 채널 설정** — 채널을 추가하거나 비용 차감 대상을 바꿀 때만.

**`이익률 계산`** 버튼 → 결과가 아래에 나옵니다.
`결과 엑셀 다운로드` 로 받거나 `결과 폴더에도 저장` 을 누르면 `결과/` 에 저장됩니다.

---

# 배치로 쓰기

1. 이카운트 파일 4개를 `입력/` 폴더에 넣기
   (파일명은 `입력/설정.xlsx` → `[채널설정]` 에 적힌 이름과 같아야 함)
2. `입력/설정.xlsx` 에 기준월·카드수수료·샘플비용 입력
3. `실행.bat` 더블클릭 → `결과/2026-08 이익률.xlsx` 생성

---

# 기준 파일 (원가·셀러수수료)

`입력/설정.xlsx` → **[기준파일]** 시트에 적힌 파일에서 원가와 셀러수수료를 읽습니다.
**위에 있는 파일이 우선**이고, 아래 파일은 위에서 못 찾은 코드만 보완합니다.

| 순서 | 이름 | 파일경로 | 사용 |
|---|---|---|---|
| 1 | 마진율표 | `G:\공유 드라이브\...\상품별마진율` (폴더) | O |
| 2 | 구마스터 | `이익률 마스터.xlsx` | O |

**1번은 구글 드라이브 원본 폴더를 직접 봅니다.** 담당자가 그 파일을 갱신하면
사이드바에서 `🔄 업데이트` 만 누르면 반영됩니다 — 복사해 올 필요가 없습니다.

## 업데이트 방법

기준 파일을 **매번 자동으로 읽지는 않습니다** (마진율표가 4.5MB라 매번 읽으면 느립니다).
대신 사이드바에서 파일 상태를 항상 볼 수 있습니다.

```
마진율표 · 파일 수정 2026-08-21 11:07
구마스터 · 파일 수정 2026-08-24 08:42
합계 원가 4,598건 · 셀러수수료 1,987건
읽은 시각 08-24 10:23:03
```

| 버튼 | 하는 일 |
|---|---|
| **🔄 업데이트** | 기준 파일을 지금 다시 읽습니다. 마진율표가 갱신됐을 때 이것만 누르면 됩니다 |
| **상태 새로고침** | 파일이 바뀌었는지만 확인합니다 (읽지 않으므로 빠름) |

파일이 바뀐 것이 감지되면 이렇게 알려줍니다.

> ⚠️ 기준 파일이 바뀌었습니다. **🔄 업데이트** 를 눌러 반영하세요.

## 다른 위치의 파일을 쓰려면

`[기준파일]` 시트의 파일경로만 바꾸면 됩니다.

| 파일이 있는 곳 | 파일경로에 적는 방법 |
|---|---|
| 구글 드라이브 (데스크톱 앱) | `G:\공유 드라이브\...\파일명.xlsx` |
| 이 폴더 | `파일명.xlsx` (파일명만) |
| 사내 공유폴더 | `\\서버이름\공유\...\파일명.xlsx` |
| 네트워크 드라이브 | `Z:\...\파일명.xlsx` |
| OneDrive / SharePoint 동기화 폴더 | `C:\Users\사용자\OneDrive - 회사\...\파일명.xlsx` |

> 그때그때 다른 파일로 계산해 보려면 사이드바의 **기준 파일 직접 올리기** 를 쓰세요.
> 올린 파일이 최우선으로 적용되고, 설정의 파일은 건드리지 않습니다.

## 마진율표에서 읽는 값

| 값 | 어느 열에서 | 어느 시트 |
|---|---|---|
| 원가 | `공급처옵션`(상품코드) + `단가` | 유통상품별마진율 / B2B상품별마진율 / 라이브방송 |
| 셀러수수료 | `셀러 마진율` | 스룩_마진율1 |
| 셀러수수료 예외 | `할인 마진율` | 업체별 예외단가 중 업체가 '스룩'인 행 (기본보다 우선) |

- 같은 상품코드가 여러 번 나오면 **기재날짜가 가장 최근인 값**을 씁니다.
  어떤 값들이 있었는지는 결과의 **[중복코드점검]** 에서 상품명·옵션명까지 볼 수 있습니다.
- `공급처옵션` 열이 없는 시트는, 코드가 있는 시트에서 만든
  **(상품명 + 옵션명) → 상품코드** 대응표로 연결합니다.
- 시트나 열이 늘어나도 **열 이름으로 찾으므로** 대부분 그대로 동작합니다.

## 중복코드 점검 — 꼭 확인하세요

마진율표에서 **하나의 `공급처옵션` 코드가 여러 옵션에 공용으로** 쓰인 경우가 있습니다.
이때 옵션마다 단가가 달라 어떤 값을 쓸지 애매해집니다.

```
T2405V0223  2026-07-21  샘플/[스네프 튜브 모음]  [워터해먹 레스트 베이지]     13,900
T2405V0223  2026-07-21  샘플/[스네프 튜브 모음]  [가디언 성인링튜브 헤이즐럿]  ★ 9,400
T2405V0223  2026-07-20  [스네프 튜브 모음]      [워터해먹 레스트 베이지]     20,016
```

프로그램은 **기재날짜가 가장 최근인 값**(★)을 쓰고, 나머지 후보를 전부 보여줍니다.
웹앱 `중복코드 점검` 탭 / 엑셀 `[중복코드점검]` 시트에서
`이번 달 판매된 것만 보기` 로 걸러 판매액 큰 순으로 확인하세요.

정확한 원가가 필요하면 마진율표에서 **옵션별로 `공급처옵션` 코드를 나누거나**,
`원가보정.xlsx` 에 그 코드의 원가를 직접 지정하면 됩니다.

---

# 결과 보는 법

| 시트 / 탭 | 내용 |
|---|---|
| **종합** | 채널별 매출·원가·수수료·이익액·이익률, 총이익률 |
| 채널별 (유통 / 유통B2B / 유통_입점몰 / 스룩) | 상품별 상세. 배송비는 따로 |
| **미매칭** | 원가를 못 찾은 상품 → 이 목록을 없애는 게 정확도의 핵심 |
| 셀러수수료 기본율 | 요율을 못 찾아 기본율(15%)로 계산된 상품 |
| 보정적용내역 | `원가보정.xlsx` 가 적용된 건. **노란 줄은 기준 파일 값을 덮어쓴 것이니 확인 필요** |
| **중복코드점검** | 같은 상품코드가 다른 원가로 여러 번 등록된 건. ★ 가 실제 사용된 값 |

채널별 상세의 **`원가출처`** 열에 그 원가를 어디서 가져왔는지 표시됩니다
(`마진율표` / `구마스터` / `보정` / `미매칭`).
엑셀에서 **빨간 줄 = 원가 미매칭**, **노란 칸 = 보정값 또는 기본율 사용**입니다.
웹앱 채널 탭에서는 `원가 미매칭만 보기` 로 걸러 볼 수 있습니다.

---

# 계산식

```
유통 / 유통B2B / 유통_입점몰
    이익 = 판매액 − (원가 × 수량)                       ※ 유통만 샘플비용 추가 차감

스룩
    이익 = 판매액 − (원가 × 수량) − 셀러수수료 − 카드수수료
           셀러수수료 = 판매액 × 요율
           카드수수료 = 결제수단별 금액 × 요율 (스룩 전체에서 1회 차감)

배송비 (품목그룹3코드 = Z0001)
    매출 = 원가 로 처리 → 이익 0.  "배송 제외 이익률"
```

**이익률 = 총이익액 ÷ 총매출(배송 포함)**

## 값이 정해지는 순서

```
원가        : 원가보정(우선적용 O) → 기준파일 1번 → 2번 → … → 원가보정(우선적용 X) → 0(미매칭)
셀러수수료   : 원가보정[셀러수수료보정] → 기준파일 1번 → 2번 → … → 기본율(15%)
```

- `원가보정.xlsx` 는 한 번 적어두면 **매달 자동 적용**됩니다.
  기준 파일이 갱신되어 값이 맞게 되면 해당 줄을 지우세요.
- `우선적용` 열: `O` = 기준 파일에 값이 있어도 이 값을 씀 / `X`(빈칸) = 없을 때만 씀

## 미매칭이 나오면

`[미매칭]` 을 열어 판매액이 큰 것부터 처리합니다.

- **원칙**: 마진율표에 해당 상품의 `공급처옵션`(상품코드)과 `단가` 를 추가 (근본 해결)
- **임시**: `원가보정.xlsx` → `[원가보정]` 에 추가하고 `우선적용` 은 `X`

원가가 없으면 그 상품의 이익이 **판매액 전액으로 잡혀 이익이 과대계상**됩니다.

## 이카운트 다운로드에 필요한 열

`품목그룹3코드 · 품목그룹3 · 품목명[규격]코드 · 품목명[규격] · 수량 · 판매액`

열 순서는 상관없고, 위에 제목/조회조건 줄이 몇 개 있어도 알아서 헤더를 찾습니다.
`품목그룹3코드` 가 있어야 배송비(`Z0001`)를 자동 분리합니다. csv 도 됩니다.

---

# 파일 구성

```
이익율/
├─ 웹앱실행.bat          ← 웹 화면으로 쓰기 (권장)
├─ 실행.bat              ← 배치로 쓰기
├─ 초기설정.bat          ← 설정 파일을 새로 만들 때만
├─ engine.py             ← 계산 로직 (웹앱·배치 공용)
├─ app.py / calc.py / launch_web.py / setup_config.py
├─ 이익률 마스터.xlsx      ← 기준 파일 2 (보완용)
│                        기준 파일 1 은 구글 드라이브 원본을 직접 참조
├─ 원가보정.xlsx          ← 기준 파일에 없는 원가·요율 수동 보정
├─ 입력/
│   ├─ 설정.xlsx         ← 기준월·기준파일·채널설정·카드수수료·샘플비용
│   └─ 유통.xlsx  유통B2B.xlsx  유통_입점몰.xlsx  스룩.xlsx   (배치용)
└─ 결과/
    └─ 2026-08 이익률.xlsx
```

> `.py` 파일 이름이 영문인 이유: cmd.exe 가 한글이 섞인 배치 파일을 잘못 읽어
> 명령어가 깨지는 문제가 있어, 배치에서 부르는 파일은 영문으로 두었습니다.

# 문제가 생기면

| 증상 | 원인 / 해결 |
|---|---|
| `python을 찾을 수 없습니다` | python.org 에서 설치. 설치 시 **Add Python to PATH** 체크 |
| 웹앱 실행 실패 | 명령창에서 `pip install streamlit openpyxl pandas` |
| 검은 창만 뜨고 브라우저가 안 열림 | 주소창에 `http://localhost:8501` 직접 입력 |
| 웹앱이 갑자기 멈춤 | 검은 창을 닫으면 종료됩니다. 다시 `웹앱실행.bat` 실행 |
| `읽을 기준 파일이 없습니다` | `입력/설정.xlsx` → `[기준파일]` 의 파일경로 확인 |
| 공유폴더 파일을 못 읽음 | 탐색기에서 그 경로가 열리는지 먼저 확인 (권한·VPN) |
| `입력/○○.xlsx 없음` (배치) | 파일명이 `설정.xlsx [채널설정]` 과 다름 |
| `열을 찾지 못했습니다` | 다운로드에 `품목명[규격]코드/수량/판매액` 열이 있는지 확인 |
| 결과 파일 저장 안 됨 | 같은 이름 파일을 엑셀에서 열어둔 채 실행했는지 확인 |
